# ==========================================
# ADAPTIVE CODE LAB - Environment Configuration
# ==========================================
# Copy this file to '.env' and fill in your actual values
# NEVER commit the actual .env file to Git!

# ==========================================
# Database Configuration
# ==========================================

# SQLite database path (relative to project root)
# Default: src/DB/Accl_DB.db
# Note: SQLAlchemy requires 'sqlite:///' prefix for relative paths
ACCL_DB_PATH=sqlite:///src/DB/Accl_DB.db

# Alternative formats:
# Absolute path: sqlite:////absolute/path/to/src/DB/Accl_DB.db
# In-memory (testing): sqlite:///:memory:

# ==========================================
# AI API Keys (REQUIRED)
# ==========================================

# Groq API Key - For AI-powered code feedback and hints
# Get your free API key at: https://console.groq.com/keys
# Free tier: 30 requests/minute, sufficient for most use cases
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini API Key - For code embeddings and plagiarism detection
# Get your free API key at: https://aistudio.google.com/app/apikey
# Free tier: 1500 requests/day
GOOGLE_API_KEY=your_google_api_key_here

# ==========================================
# Model Configuration
# ==========================================

# Groq LLM Model Selection:
# - llama-3.3-70b-versatile (RECOMMENDED: best for code understanding, 128k context)
# - llama-3.1-8b-instant (faster, lower quality, good for simple hints)
# - mixtral-8x7b-32768 (good for long code files, 32k context)
GROQ_MODEL=llama-3.3-70b-versatile

# Gemini Embedding Model (usually no need to change)
# Current: text-embedding-004 (768 dimensions, multilingual)
GEMINI_EMBEDDING_MODEL=models/text-embedding-004

# ==========================================
# AI Behavior Settings
# ==========================================

# Plagiarism Detection Threshold (0.0 to 1.0)
# Higher = stricter (0.85 means 85% similarity triggers flag)
# Recommended range: 0.75 - 0.90
PLAGIARISM_THRESHOLD=0.85

# Maximum tokens in AI responses
# Typical: 1000-2000 (longer = more detailed but slower)
MAX_TOKENS=1000

# AI Temperature (0.0 to 1.0)
# 0.0 = deterministic, factual (recommended for grading)
# 0.7 = balanced creativity (good for hints)
# 1.0 = highly creative (not recommended)
AI_TEMPERATURE=0.7

# ==========================================
# Code Execution & Security (Optional)
# ==========================================

<<<<<<< HEAD
# Sandbox directory for executing student code
# IMPORTANT: Should be isolated/containerized for security
# Default: ./sandbox (relative to project root)
SANDBOX_PATH=./sandbox

# Maximum execution time per test case (seconds)
# Prevents infinite loops and resource exhaustion
# Default: 5 seconds
=======
# Database URL if using external DB (PostgreSQL, MySQL, SQLite)
# Example SQLite: sqlite:///db/accl.db
# Example PostgreSQL: postgresql://user:password@localhost:5432/accl
DB_URL=your_database_url_here

# Path to local SQLite DB for ACCL (used if DB_URL not set)
# Relative to repo root, or absolute path if preferred
ACCL_DB_PATH=./Accl_DB.db

# ==========================================
# Code Execution Sandbox (Optional)
# ==========================================

# Directory where temporary student code files will be executed
# e.g., Docker sandbox or filesystem path
SANDBOX_PATH=/path/to/sandbox

# Maximum execution time per code run (seconds)
>>>>>>> feature/ai-sandbox
SANDBOX_TIMEOUT=5

# Maximum memory limit per execution (MB)
# Default: 256MB (matches test_cases default)
SANDBOX_MEMORY_LIMIT_MB=256

# ==========================================
# Application Settings (Optional)
# ==========================================

<<<<<<< HEAD
# Application environment (development, testing, production)
# Affects logging verbosity and error handling
APP_ENV=development

# Enable debug mode (True/False)
# WARNING: Never enable in production!
DEBUG=True

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==========================================
# Server Configuration (Optional - for future web deployment)
# ==========================================

# Flask/FastAPI server host
# Use 0.0.0.0 to allow external connections
# Use 127.0.0.1 for localhost only
SERVER_HOST=127.0.0.1

# Server port
SERVER_PORT=5000

# Secret key for session encryption (generate a random string)
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here_generate_random_string

# ==========================================
# Email Notifications (Optional - for future features)
# ==========================================

# SMTP server for sending email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_app_specific_password
# SMTP_FROM=noreply@adaptivecodelab.com

# ==========================================
# Setup Instructions
# ==========================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Get API Keys:
#    - Groq: https://console.groq.com/keys
#    - Google: https://aistudio.google.com/app/apikey
#
# 3. Update .env with your actual keys:
#    Replace all 'your_*_here' placeholders
#
# 4. Verify .gitignore contains:
#    echo ".env" >> .gitignore
#
# 5. Test configuration:
#    python -c "from dotenv import load_dotenv; import os; load_dotenv(); print('✓ Config loaded')"
#
# 6. NEVER commit your .env file to Git!
#    Check with: git status
#
# ==========================================
# Troubleshooting
# ==========================================
#
# Database connection errors:
#   - Ensure ACCL_DB_PATH has 'sqlite:///' prefix
#   - Check file permissions on src/DB/Accl_DB.db
#   - Verify database exists: ls -la src/DB/Accl_DB.db
#
# API Key errors:
#   - Verify keys are active in respective consoles
#   - Check for extra spaces or newlines
#   - Ensure no quotes around the key values
#
# Import errors:
#   - Install dependencies: pip install -r requirements.txt
#   - Verify python-dotenv is installed: pip show python-dotenv
#
=======
# 1. Copy this file and rename to '.env' (without .example)
# 2. Replace all 'your_*_here' values with your actual credentials
# 3. Never commit the .env file to version control
# 4. Add '.env' to your .gitignore
# 5. Optional settings can be left as defaults or tuned per project needs
>>>>>>> feature/ai-sandbox
